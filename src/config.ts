// API configuration
// Always prefer `VITE_API_URL`. If not provided, default to the hosted API.
// Default to the hosted API for public deployments.
export const API_BASE_URL = import.meta.env.VITE_API_URL ?? 'https://vault-finance-api.onrender.com'
